# Cấu hình SMTP để gửi email
# Copy file này thành .smtp_config và điền thông tin thực tế

# Server SMTP (Gmail mặc định)
SMTP_SERVER=smtp.gmail.com

# Port SMTP (587 cho TLS)
SMTP_PORT=587

# Email gửi (phải có App Password nếu dùng Gmail)
SMTP_USER=your-email@gmail.com

# Mật khẩu hoặc App Password
SMTP_PASSWORD=your-app-password

# Email nhận (mặc định: baophongcmu@gmail.com)
SMTP_TO=baophongcmu@gmail.com
